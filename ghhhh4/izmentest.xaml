<Page x:Class="ghhhh4.izmentest"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:ghhhh4"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="izmentest">

    <Grid>


        <DataGrid x:Name="datagrid" SelectionChanged="DataGrid_SelectionChanged" ItemsSource="{Binding List}" AutoGenerateColumns="True">
            <DataGrid.Columns>
                <DataGridTextColumn  Binding="{Binding Name}"/>
                <DataGridTextColumn Binding="{Binding zadanie}" />
                <DataGridTextColumn  Binding="{Binding perotvet}" />
                <DataGridTextColumn  Binding="{Binding vtorotvet}" />
                <DataGridTextColumn  Binding="{Binding tritiotvet}" />
                <DataGridTextColumn  Binding="{Binding pravotvet}" />

            </DataGrid.Columns>

        </DataGrid>



    </Grid>
</Page>
